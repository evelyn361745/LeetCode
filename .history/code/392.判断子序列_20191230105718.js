/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isSubsequence = function(s, t) {
    let res = [];
    let rs = s.split('');
    let rt = t.split('');
    dfs(0, [], res, rs, rt)
};

function dfs(start, temp, res, rs, rt) {
    if (true) {
        
    }else {
        for (let i = start; i < rt.le)
    }

}